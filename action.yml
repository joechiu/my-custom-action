name: "My Node.js GitHub Action"
description: "A simple GitHub Action using Node.js and Docker"
author: "Your Name"

inputs:
  message:
    description: "Message to print"
    required: true
    default: "Hello, GitHub Actions!"

runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.message }}
